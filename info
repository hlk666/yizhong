部署的时候修改path的路径。

工作状态数据在文件里。

未监护，可以设置开始监护
监护中，可以设置结束监护

客户端应该不能修改监护时长，从异常模式参数设置那里去掉

设置开始监护时间，计算结束监护时间
如果中途收到或者自动结束监护，清空状态。

某个人处于监护状态下的时候，不结束监护，不能随便切换模式。1,2=>3
强制报警参数的几个值
非强制报警参数的几个值

收到或者自动判断结束监护时要回复状态数据

用监护结束时间来判断
用表管理监护结束时间的状态
发送结束命令的时候更新此表(医生端-setCommand接口-getCommand接口自动结束)

所有结束监护的地方，都要将数据插入到history表里

p_sn的问题
年龄的问题，改成只能数字，长度3位

医生端系统和分析系统的配置文件的设置

做个批处理，自动结束监护(防止手机端提前关机，不访问get_command，永远结束不了)
开单室提供强制停止上次监护的功能。

html中的标签错误
开单医生的修改

客户端直接访问数据库的表和字段的整理！！！！！！！！！！！！！！！！！！！！！
illsum.php收到的参数到底是监护id还是病号id。

双击的事件不能正确相应。

以后的内容之一：删除操作只是移动到history表里(设置删除flag)，然后设置删除原始表
所有数据库操作的地方都要判断返回值！！！

用1毫伏的电压发出标准波形，图形是10毫米，来尝试增益信息。

远程查房、远程设置参数没做！！！

下诊断时，医生签名(输入)，应该改成下拉选择。
